import styled from "styled-components";

// 模版字符串相当于函数调用
export const HeaderWrapper = styled.div`
    display: flex;
    align-items: center;
    height: 80px;
    border-bottom: 1px solid #eee;
    /* 让 left 和 right flex为1，中间input不给宽度，让中间input可以水平居中 */
    
`